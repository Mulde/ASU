package MHVDKASU;

import java.io.IOException;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.net.Socket;
import java.net.UnknownHostException;


public class socket {
	// declaring the enums for the commands
	public enum addperson{String};
	public enum adddrink{String};
	public enum cheers{String};
	
	public server(String[] args) throws UnknownHostException, IOException {
		
		// connecting the socket to the server and getting the inputstream
		Socket sck = new Socket("localhost",8090);
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		// echo server code
		/*System.out.println("enter string");
		String s = br.readLine();
		sck.getOutputStream().write(s.getBytes());
		byte[] b = new byte[1024];
		sck.getInputStream().read(b );
		System.out.println(new String(b));*/
		// sck.close();
	}

}
